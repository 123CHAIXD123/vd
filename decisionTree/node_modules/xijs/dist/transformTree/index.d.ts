/**
 * 将扁平数组转换成树结构
 * @param list 扁平数组
 * @param options 树解析map
 * @returns
 */
declare function transformTree(list?: any[], options?: {
    keyField: string;
    childField: string;
    parentField: string;
}): any[];
export default transformTree;
